export type arikAirDataType = Array<{
  departureTime: string;
  arrivalTime: string;
  departurePort: string;
  arrivalPort: string;
  flightNumber: string;
  flightDuration: string;
  price: string;
  seatsRemaining: string;
}>;
